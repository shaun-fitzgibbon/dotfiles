[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[user]
	name = {{ .fullname | quote }}
	email = {{ .email | quote }}
	username = {{ .githubUsername | quote }}
	signingkey = 871A87BD54666BA0
[init]
	defaultBranch = main
[commit]
 	gpgsign = false
[gpg]
 	program = gpg2
[core]
  pager = less -F -X
